import React from "react";
import { LoadingComponentProps } from "react-loadable";

const LoadingPage: React.ComponentType<LoadingComponentProps> = () => {
  return <>... Loading</>;
};

export default LoadingPage;
